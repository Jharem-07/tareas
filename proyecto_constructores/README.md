# Proyecto Constructores y Destructores en Python

Este proyecto demuestra el uso de constructores (__init__) y destructores (__del__) 
aplicando arquitectura de Modelos y Servicios.

## Ejecución
Ejecutar el archivo:
python main.py

## Evidencia de __init__ y __del__
- __init__: se usa al crear libros y la biblioteca.
- __del__: se ejecuta cuando los objetos dejan de existir.
